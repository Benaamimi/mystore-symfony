{% extends 'base.html.twig' %}

{% block title %}MyStore - Accueil
{% endblock %}

{% block body %}
	<div class="container mt-4">
		<h1 class="my-4">MyStore</h1>
		<h5 class="mt-4">Lorem ipsum dolor sit amet consectetur adipisicing elit. Sapiente consectetur error excepturi explicabo maxime architecto doloribus dolorum aliquid rerum minima.</h5>

		{% if not app.user %}
			<a href="{{path('user_add')}}" class="btn btn-primary my-4">Inscription</a>
		{% endif %}
	</div>
	<h1 class="mt-5 container">Boutique</h1>
	<div class="mt-4 row gx-3">
		{% for product in products %}

			<div class="col-md-3 container">
				<div class="card border-primary mb-3" style="max-width: 20rem;">
					<div class="card-header"><img src="{{ asset(product.image) }}" alt="" class="img-fluid"></div>
					<div class="card-body">
						<h4 class="card-title">{{product.title}}</h4>
						<p class="card-text">{{ product.description }}
						</p>
						<p class="card-text">{{ product.price }}
							€</p>
						<a href="{{ path('cart_add', {id: product.id}) }}" class="btn btn-primary">Ajoutez au panier</a>
					</div>
				</div>
			</div>
		{% endfor %}
	</div>

{% endblock %}
